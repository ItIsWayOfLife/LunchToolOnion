@model Web.Models.Account.LoginViewModel

@{
    ViewData["Title"] = @SharedLocalizer["ViewAccountLogin_Title"];
}
    <h1 class="text-center">@SharedLocalizer["Login"]</h1>
<br />
<div class="text-center">
    <form method="post" asp-controller="Account" asp-action="Login"
          asp-route-returnUrl="@Model.ReturnUrl">
        <div asp-validation-summary="All"  class="text-danger"></div>
        <div>
            <label asp-for="Email" class="control-label"></label><br />
            <input asp-for="Email" class="form-control"  />
        </div>
        <div>
            <label asp-for="Password" class="control-label"></label><br />
            <input asp-for="Password" class="form-control" />
        </div>
        <div>
            <label asp-for="RememberMe"></label>
            <input asp-for="RememberMe"/>
        </div>
        <div>
            <input type="submit" value="@SharedLocalizer["ComeIn"]"  class="btn btn-sm btn-success" />
        </div>
    </form>
</div>


